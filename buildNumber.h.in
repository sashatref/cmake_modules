#pragma once

#define COMPILE_DATE "${COMPILE_DATE}"
#define COMPILE_TIME "${COMPILE_TIME}"
#define COMPILE_DATETIME "${COMPILE_DATE} | ${COMPILE_TIME}"
#define FULL_VER "${PROJECT_VERSION}.${BUILD_VERSION}"
#define PRODUCT_VER "${PROJECT_VERSION}"
